# Python code run method is -> python <filename.py> / python3 <filename.py> / py <filename.py>
------------------------------------------------

# In Python Escape Character :-
    # # 1. \'   -> It is used to print a single quote. Example: 'It\'s nice' prints: It's nice
    # 2. \\   -> It is used to print a backslash. Example: 'A\\B' prints: A\B
    # 3. \n   -> It is used to create a new line. Example: 'Hi\nHello' prints:  Hi
                                                                        #  Hello on separate lines
    # 4. \r   -> It moves the cursor to the start of the line and overwrites old text with new text. Example: "Hello\rWorld!" prints: World!
    # 5. \t   -> It is used to add a big space (Tab). Example: 'A\tB' prints: A    B
    # 6. \b   -> It works like a backspace key and deletes the letter just before it. Example: "Hello \bWorld!" prints: HelloWorld!
    # 7. \f   -> It is used to force a page break, mostly for old printers.
    # 8. \ooo -> It creates a character using a 3-digit Octal number. Example: '\110' prints: H
    # 9. \xhh -> It creates a character using a 2-digit Hex number. Example: '\x48' prints: H
---------------------------------------------------------

# In Python Comments are used this simbol -> '#'. 
--------------------------------------------------

# In Python do a multiline comment:
"""
This is a multiline comment in Python.
You can write as many lines as you want.
"""
----------------------------------------------------

# python Data Types are:
    # 1. Text Type: str -> "Hello World"
    # 2. Numeric Types: int -> 20, float -> 20.5, complex -> 1j
    # 3. Sequence Types: list -> [1, 2, 3], tuple -> (1, 2, 3), range -> range(1, 10)
    # 4. Mapping Type: dict -> {"key": "value"}
    # 5. Set Types: set -> {1, 2, 3}, frozenset -> frozenset({1, 2, 3})
    # 6. Boolean Type: bool -> True or False
    # 7. Binary Types: bytes -> b"Hello", bytearray -> bytearray(b"Hello"), memoryview -> memoryview(bytes(10))
    # 8. None Type: NoneType -> None
----------------------------------------------------

# python Operators:
    # 1. Arithmetic Operators: +, -, *, /, %, **, //
    # 2. Assignment Operators: =, +=, -=, *=, /=, %=, **=, //=
--------------------------------------------------------------

# typecasting is the process of converting the data type of a value to another data type. 
-----------------------------------------------------------------------------

# String Methods :
#  (i)1_String
    # 1. string indexing -> it helps which character did you print.
    # 2. Looping Through ->  it helps in string all character print line by line.
# (ii)2_Slicing_strings 
    # 1. Slicing strings -> it helps the start index and the end index, separated by a colon, to return a part of the string.
# (iii)3_String_Methods 
    # 1. len(), upper(), lower(), rstrip(), replace(), strip()->{lstrip, rstrip}, split(), capitalize(), center(), count(), endswith(), find(), index(), isalnum(), isalpha(), islower(), isprintable(), isspace(), istitle(), isupper(), startswith(), swapcase(), title()


#    { # join()
# partition()
# rpartition()
# splitlines()
# zfill()
# casefold()
# removeprefix()    # Python 3.9+
# removesuffix()    # Python 3.9+
# format()
# format_map()
# encode()
# expandtabs()
# maketrans()
# translate()
# rfind()
# rindex()} -> ai gulo baki asa korta hoba
-----------------------------------------------------------------------------

4_Conditional:-

    # Conditional Operators :- 
        # 1. == -> 
        # 2. != ->
        # 3. < ->
        # 4. > ->
        # 5. <= ->
        # 6. >= ->

    # Indentation in Python:
        # Python does not use curly brackets {} to define code blocks. 
        # Instead, it uses a specific amount of whitespace (1 Tab or 4 Spaces).

        # It is used after keywords like if/else, loops, or functions to show 
        # which lines of code belong to that specific block.

        # If you miss or mess up the spaces, Python will throw an 'IndentationError'.   

    # Quick Example:
        if 5 > 2:
        print("Five is greater than two!") # This indent (space) is mandatory 

    # normali condition chek so use -> if(), else(). now in python when user chek multiple condition so there use -> if(), elif(), else().
-----------------------------------------------------------------------------

5_Match_Case:-
    # "In Python, match-case is a feature similar to the switch-case found in other programming languages (which was first introduced in Python version 3.10).
    # Simply put, instead of writing multiple if-elif-else conditions, match-case is used to make the code cleaner, more organized, and easier to read. This is known as Structural Pattern Matching."
-----------------------------------------------------------------------------

6_Loops:-
    # In Python, there are two types of loops:    
        # 1. for loop -> It is used to iterate over a sequence (like a list, tuple, dictionary, set, or string) and execute a block of code for each item in the sequence.
        # 2. while loop -> It is used to execute a block of code as long as a specified condition is true.
-----------------------------------------------------------------------------

7_Functions:-
    # In Python, a function is a block of code that is designed to perform a specific task. 
    # Functions help break our program into smaller and modular chunks. As our program grows larger and larger, functions make it more organized and manageable.
    # It avoids repetition and makes the code reusable.
    # in Python, a function is defined using the def keyword, followed by the function name and parentheses ().
        
        # pass Statement
            # Function definitions cannot be empty. If you need to create a function placeholder without any code, use the pass statement

 • Python Function Arguments types are:
    # 1. Default Arguments => Default arguments are values assigned to function parameters when the function is defined. If you call the function without providing a value for that parameter, Python automatically uses this pre-defined default value.
    # 2. Keyword Arguments => Keyword arguments allow you to specify the values of function parameters by explicitly naming them in the function call. This makes the code more readable and allows you to provide arguments in any order, as long as you use the parameter names.
    # 3. Required Arguments => Required arguments are the parameters that must be provided when calling a function. If you don't provide a value for a required argument, Python will raise a TypeError, indicating that the function call is missing a required positional argument.
    # 4. Variable-length Arguments => Variable-length arguments allow you to pass a variable number of arguments to a function. In Python, you can use *args for non-keyword variable-length arguments and **kwargs for keyword variable-length arguments. This is useful when you don't know in advance how many arguments will be passed to the function.
-----------------------------------------------------------------------------
    
8_Lists_Tuples:-
    # 1. Lists -> A list is a collection which is ordered and changeable. In Python lists are written with square brackets / [].
    # 2. Tuples -> A tuple is a collection which is ordered and unchangeable. In Python tuples are written with round brackets / ().

    • Python List Methods are:
        # append() -> It adds an element at the end of the list.
        # sort() -> It sorts the list in ascending order.
        # reverse() -> It reverses the order of the list.
        # index() -> It returns the index of the first occurrence of the specified value.
        # count() -> It returns the number of occurrences of the specified value.
        # copy() -> It returns a shallow copy of the list.
        # insert() -> It inserts an element at the specified position.
        # extend() -> It adds the elements of a list (or any iterable), to the end of the current list.

    {
        List Slicing, List Comprehension - Advanced, Iterating Techniques, Nested Lists / 2D Arrays, Tuples, Dictionaries
    } -> ai gulo baki asa korta hoba
