# Python code run method is -> python <filename.py> / python3 <filename.py> / py <filename.py>
------------------------------------------------

# In Python Escape Character :-
    # # 1. \'   -> It is used to print a single quote. Example: 'It\'s nice' prints: It's nice
    # 2. \\   -> It is used to print a backslash. Example: 'A\\B' prints: A\B
    # 3. \n   -> It is used to create a new line. Example: 'Hi\nHello' prints:  Hi
                                                                        #  Hello on separate lines
    # 4. \r   -> It moves the cursor to the start of the line and overwrites old text with new text. Example: "Hello\rWorld!" prints: World!
    # 5. \t   -> It is used to add a big space (Tab). Example: 'A\tB' prints: A    B
    # 6. \b   -> It works like a backspace key and deletes the letter just before it. Example: "Hello \bWorld!" prints: HelloWorld!
    # 7. \f   -> It is used to force a page break, mostly for old printers.
    # 8. \ooo -> It creates a character using a 3-digit Octal number. Example: '\110' prints: H
    # 9. \xhh -> It creates a character using a 2-digit Hex number. Example: '\x48' prints: H
---------------------------------------------------------

# In Python Comments are used this simbol -> '#'. 
--------------------------------------------------

# In Python do a multiline comment:
"""
This is a multiline comment in Python.
You can write as many lines as you want.
"""
----------------------------------------------------

# python Data Types are:
    # 1. Text Type: str -> "Hello World"
    # 2. Numeric Types: int -> 20, float -> 20.5, complex -> 1j
    # 3. Sequence Types: list -> [1, 2, 3], tuple -> (1, 2, 3), range -> range(1, 10)
    # 4. Mapping Type: dict -> {"key": "value"}
    # 5. Set Types: set -> {1, 2, 3}, frozenset -> frozenset({1, 2, 3})
    # 6. Boolean Type: bool -> True or False
    # 7. Binary Types: bytes -> b"Hello", bytearray -> bytearray(b"Hello"), memoryview -> memoryview(bytes(10))
    # 8. None Type: NoneType -> None
----------------------------------------------------

# python Operators:
    # 1. Arithmetic Operators: +, -, *, /, %, **, //
    # 2. Assignment Operators: =, +=, -=, *=, /=, %=, **=, //=
--------------------------------------------------------------

# typecasting is the process of converting the data type of a value to another data type. 
-----------------------------------------------------------------------------

# String Methods :
#  (i)1_String
    # 1. string indexing -> it helps which character did you print.
    # 2. Looping Through ->  it helps in string all character print line by line.
# (ii)2_Slicing_strings 
    # 1. Slicing strings -> it helps the start index and the end index, separated by a colon, to return a part of the string.
# (iii)3_String_Methods 
    # 1. len(), upper(), lower(), rstrip(), replace(), strip()->{lstrip, rstrip}, split(), capitalize(), center(), count(), endswith(), find(), index(), isalnum(), isalpha(), islower(), isprintable(), isspace(), istitle(), isupper(), startswith(), swapcase(), title()


#    { # join()
# partition()
# rpartition()
# splitlines()
# zfill()
# casefold()
# removeprefix()    # Python 3.9+
# removesuffix()    # Python 3.9+
# format()
# format_map()
# encode()
# expandtabs()
# maketrans()
# translate()
# rfind()
# rindex()} -> ai gulo baki asa korta hoba
-----------------------------------------------------------------------------

4_Conditional:-

    # Conditional Operators :- 
        # 1. == -> 
        # 2. != ->
        # 3. < ->
        # 4. > ->
        # 5. <= ->
        # 6. >= ->

    # Indentation in Python:
        # Python does not use curly brackets {} to define code blocks. 
        # Instead, it uses a specific amount of whitespace (1 Tab or 4 Spaces).

        # It is used after keywords like if/else, loops, or functions to show 
        # which lines of code belong to that specific block.

        # If you miss or mess up the spaces, Python will throw an 'IndentationError'.   

    # Quick Example:
        if 5 > 2:
        print("Five is greater than two!") # This indent (space) is mandatory 

    # normali condition chek so use -> if(), else(). now in python when user chek multiple condition so there use -> if(), elif(), else().
-----------------------------------------------------------------------------


